# Runtime dependencies — pinned with compatible-release specifiers so CI and
# production builds are reproducible while still receiving security patches.
requests==2.33.0
python-dotenv==1.2.2
geojson==3.1.0
